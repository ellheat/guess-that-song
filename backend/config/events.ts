export enum Events {
  Connection = 'connection',
  Disconnect = 'disconnect',
  PlayersList = 'playersList',
}

export enum PlayerEvents {
  Add = 'playerAdd',
  Added = 'playerAdded',
  Ready = 'playerReady',
  Data = 'playerData',
}
