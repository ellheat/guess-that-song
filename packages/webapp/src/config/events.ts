export enum Events {
  GameState = 'gameState',
  PlayersList = 'playersList',
}

export enum PlayerEvents {
  Add = 'playerAdd',
  Added = 'playerAdded',
  Data = 'playerData',
  Ready = 'playerReady',
}

export enum QuizEvents {
  Round = 'round',
  RoundTimer = 'roundTimer',
}
